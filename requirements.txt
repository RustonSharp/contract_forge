# 基础 Web 框架 (升级到最新稳定版)
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# 向量数据库与 AI 逻辑
chromadb==0.5.0
numpy<2.0.0
sentence-transformers>=3.0.0
langchain-core>=0.3.0
langgraph>=0.2.0
openai>=2.14.0

# 文档解析与处理
pymupdf==1.24.11
python-docx==1.1.2

# OCR 支持（可选，根据需要安装）
# pytesseract>=0.3.10  # Tesseract OCR Python 包装器
# Pillow>=10.0.0  # 图像处理库（OCR 需要）
# paddleocr>=2.7.0  # 百度 PaddleOCR（中文识别效果好）

# 实用工具
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.32.0
aiohttp>=3.9.0