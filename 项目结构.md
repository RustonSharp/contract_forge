contract_forge/
├── 📂 backend/ # Python 后端核心代码 (FastAPI + LangGraph)
│ ├── 📂 api/ # API 接口层
│ │ ├── **init**.py
│ │ ├── routes.py # 接收文件上传、查询审计状态、返回风险报告
│ │ └── schemas.py # Pydantic 模型 (ContractSummary, RiskItem)
│ │
│ ├── 📂 core/ # 业务逻辑 (LangGraph 编排)
│ │ ├── **init**.py
│ │ ├── graph.py # 核心流程定义: Start -> Parse -> Retrieve -> Eval -> End
│ │ ├── nodes.py # 流程节点具体函数实现
│ │ └── state.py # ContractState 定义 (管理解析文本、风险点、进度状态)
│ │
│ ├── 📂 tools/ # 核心算力工具集
│ │ ├── **init**.py
│ │ ├── base.py # BaseTool 抽象基类
│ │ ├── doc_parser.py # 基于 PyMuPDF 的文本提取
│ │ ├── law_retriever.py # 基于 ChromaDB 的语义检索
│ │ └── risk_evaluator.py # 基于 LLM 的风险判定逻辑
│ │
│ ├── 📂 data/ # 持久化存储
│ │ └── 📂 chroma_db/ # ChromaDB 向量数据库文件 (自动生成)
│ │
│ ├── 📂 prompts/ # LLM 提示词模板
│ │ ├── extraction.yaml # 提取金额、日期、违约金比例的 Prompt
│ │ └── evaluation.yaml # 对比法条判定风险的 Prompt
│ │
│ ├── .env # API_KEY, DATABASE_URL 等
│ ├── main.py # FastAPI 启动入口 (uvicorn main:app)
│ └── requirements.txt # 依赖清单
│
├── 📂 n8n/ # N8N 流程配置 (作为外部触发器或通知器)
│ └── contract_audit_flow.json
│
├── 📂 frontend/ # React 前端 (Vite + Tailwind v4)
│ ├── 📂 src/
│ │ ├── 📂 components/
│ │ │ ├── ContractUpload.tsx # 文件上传与进度展示
│ │ │ ├── RiskDashboard.tsx # 风险结果可视化看板
│ │ │ └── LawReference.tsx # 关联法条弹窗组件
│ │ ├── 📂 api/
│ │ │ └── client.ts # Axios 封装
│ │ └── App.tsx
│ ├── index.css # 只有一行 @import "tailwindcss";
│ └── package.json
│
├── 📂 tests/ # 单元测试与集成测试
│ ├── test_parser.py # 测试 PDF 解析是否准确
│ └── test_retriever.py # 测试 ChromaDB 检索是否匹配
│
└── 📂 uploads/ # 临时存放用户上传的待审合同
